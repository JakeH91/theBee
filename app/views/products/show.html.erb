<h2 class="product-name">
  <strong><%= @product.name %></strong> 
</h2>
	
<%= image_tag(@product.image_url.to_s, class: "img-responsive product-image") %>

<div class="product-info">	
	<p class="product-description">
	  <%= @product.description %>
	</p>

	<%# if @product.color != nil %>
		<!-- <p class="product-colour">
			Colour: <%= @product.color %>
		</p> -->
	<%# end %>

	<% if @product.price != nil %>
		<p class="product-price">
			Price: $<%= @product.price %>
		</p>
	<% end %>
</div>


<%= render partial: "new_comment", comment: @comment %>
<%= render partial: "comments", comment: @comment %>
<!-- <div class="product-reviews">
	<h3>Reviews</h3>
	<p>Haven't quite made up your mind? Here's what some helpful people had to say about this item:</p>
	<div class="reviews">
		<p>This is lovely!</p>
	</div>
</div> -->

<p class="product-options">
	<%= link_to 'Edit', edit_product_path(@product) %>|
	<%= link_to 'Back', :back %>
</p>


